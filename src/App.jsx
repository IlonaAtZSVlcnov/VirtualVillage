import {useState} from "react";import {locations} from "./data/lessons.js";import VillageMap from "./components/VillageMap.jsx";export default function App(){const [sel,setSel]=useState(null);const [cat,setCat]=useState("All");const cats=["All",...new Set(locations.map(x=>x.category))];const filtered=cat==="All"?locations:locations.filter(x=>x.category===cat);return <div className="container"><h1>🏘️ Virtual Village</h1>{!sel&&<><select value={cat} onChange={e=>setCat(e.target.value)}>{cats.map(c=><option key={c} value={c}>{c}</option>)}</select><VillageMap locations={filtered} onSelect={setSel}/></>}{sel&&<div className="lesson"><button onClick={()=>setSel(null)}>Back</button><h2>{sel.professionEN}</h2><p>{sel.professionDE}</p><p>{sel.category}</p><p>{sel.level}</p><p>{sel.xp} XP</p></div>}</div>}