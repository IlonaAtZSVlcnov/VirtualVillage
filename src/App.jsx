import {useState} from "react";import {locations} from "./data/lessons";import VillageMap from "./components/VillageMap";export default function App(){const[s,setS]=useState(null);const[c,setC]=useState("All");const cats=["All",...new Set(locations.map(x=>x.category))];const data=c==="All"?locations:locations.filter(x=>x.category===c);return <div className="container"><h1>Virtual Village</h1>{!s&&<><select value={c} onChange={e=>setC(e.target.value)}>{cats.map(x=><option key={x}>{x}</option>)}</select><VillageMap locations={data} onSelect={setS}/></>}{s&&<div className="lesson"><button onClick={()=>setS(null)}>Back</button><h2>{s.professionEN}</h2><p>{s.professionDE}</p><p>{s.category}</p><p>{s.level}</p><p>{s.xp} XP</p></div>}</div>}